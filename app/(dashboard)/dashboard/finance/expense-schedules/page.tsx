import { ExpenseSchedulesView } from "@/app/components/dashboard/finance/expense-schedules-view";

export const metadata = {
    title: "Recurring expenses | VentraPOS",
};

export default function ExpenseSchedulesPage() {
    return <ExpenseSchedulesView />;
}
