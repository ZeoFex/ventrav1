/** Referral rewards: 2% (200 bps) per qualified referee, max 20% (2000 bps). */
export const REFERRAL_REWARD_BPS_PER_REFEREE = 200;
export const REFERRAL_MAX_REWARD_BPS = 2000;

/** Cookie name for pre-signup referral attribution (?ref=). */
export const REFERRAL_COOKIE_NAME = "ventra_ref";
export const REFERRAL_COOKIE_MAX_AGE_SEC = 90 * 24 * 60 * 60;
